[package]
name = "herakles-node-exporter"
version = "0.1.0"
description = "Comprehensive Prometheus exporter for Linux system metrics including per-process memory/CPU, disk I/O, filesystem, and network statistics"
authors = ["Michael Moll <exporter@herakles.now>"]
license = "Apache-2.0"
edition = "2021"
repository = "https://github.com/herakles-now/herakles-node-exporter"
readme = "README.md"
keywords = ["prometheus", "exporter", "memory", "metrics", "process"]
categories = ["command-line-utilities", "monitoring"]

[features]
default = ["ebpf"]
# Enable eBPF-based metrics collection
ebpf = ["libbpf-rs", "libbpf-sys"]
# Enable eBPF with vendored libelf/zlib for musl/static builds
ebpf-vendored = ["ebpf", "libbpf-sys/vendored"]

[dependencies]
axum = { version = "0.8", features = ["http1", "macros", "tokio"] }
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive"] }
prometheus = "0.14"
rayon = "1.8"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1.0"
anyhow = "1.0"
once_cell = "1.21.3"
toml = "1.1"

# LOGGING DEPENDENCIES
tracing = "0.1"
tracing-subscriber = "0.3"

# Optional: For better performance
ahash = "0.8"

# Concurrent HashMap for ringbuffer management
dashmap = "6.1"
sled = "0.34"


# System calls for runtime configuration (e.g., clock ticks)
libc = "0.2"

# Random number generation for testdata
rand = "0.10"

# DateTime handling for testdata timestamp
chrono = { version = "0.4", features = ["serde"] }

# TLS support
axum-server = { version = "0.8", features = ["tls-rustls"] }

# eBPF support (optional - feature gated)
libbpf-rs = { version = "0.26", optional = true }
libbpf-sys = { version = "1.4", optional = true }

# System user/group management for install command
nix = { version = "0.31", features = ["user", "process", "fs"] }

[build-dependencies]
vergen = { version = "10.0.0", features = ["build"] }
vergen-gitcl = { version = "10.0.0", features = ["build"] }

[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dev-dependencies]
tempfile = "3.23.0"
