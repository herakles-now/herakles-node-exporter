/target

# Binary output directory
/binary

# eBPF generated files
src/ebpf/bpf/vmlinux.h
# eBPF compiled objects are build artifacts: build.rs recompiles process_io.bpf.c
# and copies the object into the src tree on every ebpf build, so it is not tracked.
*.bpf.o

# CodeQL artifacts
_codeql_detected_source_root
